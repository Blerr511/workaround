import { ProjectItem } from "@wr/ui/src/components";

const ProjectList = () => {

  

  return (
    <main>
      <ProjectItem id={1} title="Test" updatedAt={new Date()} />
    </main>
  );
};

export default ProjectList;
