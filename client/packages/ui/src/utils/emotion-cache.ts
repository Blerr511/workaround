'use client';

import createCache from '@emotion/cache';
import createEmotionServerNative from '@emotion/server/create-instance';

// const cache = createCache({
//   key: 'css',
// });

// export const createEmotionServer = () => createEmotionServerNative(cache);
