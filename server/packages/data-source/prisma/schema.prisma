datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:qwertyu1234567@localhost:6432/wr"
}

generator client {
  provider      = "prisma-client-js"
  output        = "prisma-client"
}

model MyUser {
  id                                          Int                                      @id @default(autoincrement())
  name                                        String
  lastName                                    String
  email                                       String                                   @unique
}
